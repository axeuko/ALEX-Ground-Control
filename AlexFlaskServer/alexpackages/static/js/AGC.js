function forward(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}

function left(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}

function backward(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}

function right(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}

function stop(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}

function speedup(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}

function slowdown(pycmd){
    var request = new XMLHttpRequest()
    request.open("GET", "/" + pycmd, true)
    request.send()
}